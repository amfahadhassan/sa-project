server:
  port: 8080
spring:
  main:
    web-application-type: reactive
  cloud:
    gateway:
      routes:
        - id: presentationService
          uri: http://localhost:8092/export/
          predicates:
            - Path= /export/**